import { ConvertToLowerPipe } from './convert-to-lower.pipe';

describe('ConvertToLowerPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToLowerPipe();
    expect(pipe).toBeTruthy();
  });
});
